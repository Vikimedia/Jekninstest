pipeline { 
    agent { label 'Scanner' }
    stages {
        stage('Build') { 
            steps { 
                sh 'echo "blabla" > /home/vagrant/start/newfile' 
            }
        }
      
    }
}
